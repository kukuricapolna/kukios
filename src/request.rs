use core::str::FromStr;

use smoltcp::wire::IpAddress;

pub struct Request;

impl Request {
    pub fn get(url: &str) -> Result<&str, &'static str> {
        // let address = IpAddress::from_str()

        todo!();
    }
}
